{
  "name": "codeswarm-workspace",
  "private": true,
  "workspaces": [
    "cli",
    "web/backend",
    "web/frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build:cli": "npm --workspace=cli run build",
    "build:backend": "npm --workspace=web/backend run build",
    "build:frontend": "npm --workspace=web/frontend run build",
    "build:release": "npm run build:cli && npm run build:backend && npm run build:frontend && python3 tools/build_release_bundle.py",
    "link:cli": "npm --workspace=cli link",
    "bootstrap": "npm install && npm --workspace=frontend run build && npm --workspace=cli run build && npm --workspace=cli link",
    "dev:web": "codeswarm web --config configs/hpcfund.json",
    "build:all": "npm run build:cli && npm run build:backend && npm run build:frontend",
    "test:web-ui": "node --test tools/web_ui/browser.test.cjs",
    "test:project-runtime-smoke": "python3 tools/orchestrated_project_runtime_smoke.py --planner-runtime mock --worker-runtime mock --mode both"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
